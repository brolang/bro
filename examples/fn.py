


module Hello {
  type hello: int {
    h: string
    
    hello(): int else error {
       if ell == "string" {
          print ell
       } 

       if b == "call" {
          x: int = x + 2
          //lambda syntax
          x(z: int, b: int) = z + b;
          loop i != 0 {

          }

          loop x: range(0,20) {
            print x+1
          } 

          mixin = {

          }
       }
    }

    fn say() {
       print "Hello"
    }
  }

  fn hello(x: int,y: string): int {
     x = 2
  }
}
